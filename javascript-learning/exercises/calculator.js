const add = (a, b) => {
    const result = a + b;
    console.log(`Výsledek sčítání: ${a} + ${b} = ${result}`);
};

const subtrack = (a, b) => {
    const result = a - b;
    console.log(`Výsledek odčítání: ${a} - ${b} = ${result}`);
};

function multiply(a, b) {
    const result = a * b;
    console.log(`Výsledek násobení: ${a} * ${b} = ${result}`);
}

function divide(a, b) {
    const result = a / b;
    console.log(`Výsledek dělení: ${a} / ${b} = ${result}`);
}

add(15, 48);
subtrack(98, 41);
multiply(54, 30);
divide(88, 6);


function add2(a, b) {
    const result = a + b;
    console.log("Výsledek sčítání: " + result);
}

function subtrack2(a, b) {
    const result = a - b;
    console.log("Výsledek odčítání: " + result);
}

function multiply2(a, b) {
    const result = a * b;
    console.log("Výsledek nádobení: " + result);
}

function divide2(a, b) {
    const result = a / b;
    console.log("Výsledek dělení: " + result);
}

add2(15, 48);
subtrack2(98, 41);
multiply2(54, 30);
divide2(88, 6);
 